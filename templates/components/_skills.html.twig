<section id="skills" class="mt-12 lg:mt-16 max-w-3xl mx-auto lg:mx-0">
    <h2 class="text-3xl lg:text-4xl font-bold mb-6">Skills</h2>
    
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        {% for category, data in skills %}
            <div class="rounded-xl bg-gradient-to-br from-primary/5 to-base-300/50 backdrop-blur-sm border border-base-content/5 p-4">
                <h3 class="text-lg font-semibold mb-3 text-base-content/70 flex items-center gap-2">
                    <i class="{{ data.icon }} text-primary"></i>{{ category }}
                </h3>
                <div class="flex flex-wrap gap-2">
                    {% for item in data.items %}
                        {% if item.display is defined and item.display %}
                            <div class="badge badge-md px-2 py-2 bg-{{ data.color_bg }} text-{{ data.color_text }} hover:bg-{{ data.color_hover }} transition-colors">
                                {% if item.icon is defined %}
                                    <i class="{{ item.icon }} mr-1"></i>
                                {% else %}
                                    <img src="{{ asset('images/' ~ item.image) }}" alt="{{ item.name }}" class="w-3 h-3 mr-1">
                                {% endif %}
                                {{ item.name }}
                            </div>
                        {% endif %}
                    {% endfor %}
                </div>
            </div>
        {% endfor %}
    </div>
</section>
