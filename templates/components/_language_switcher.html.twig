<div class="fixed top-4 right-4 z-50">
    {% set languages = [
        { code: 'en', name: 'English', flag: '🇬🇧' },
        { code: 'fr', name: 'Français', flag: '🇫🇷' }
    ] %}
    
    <div class="dropdown dropdown-end">
        <label tabindex="0" class="btn btn-ghost m-1">
            {% for lang in languages %}
                <span class="language-flag language-flag-{{ lang.code }} text-lg" style="display: {% if app.request.locale == lang.code %}inline{% else %}none{% endif %}">{{ lang.flag }}</span>
            {% endfor %}
        </label>
        <ul tabindex="0" class="dropdown-content z-[1] menu p-2 shadow bg-base-100 rounded-box w-32">
            {% for lang in languages %}
                <li>
                    <a href="#" data-locale="{{ lang.code }}" class="flex items-center gap-2 {% if app.request.locale == lang.code %}active{% endif %}">
                        <span class="text-lg">{{ lang.flag }}</span>
                        <span>{{ lang.name }}</span>
                    </a>
                </li>
            {% endfor %}
        </ul>
    </div>
</div>
