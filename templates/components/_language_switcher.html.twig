<div class="fixed top-4 right-4 z-50">
    {% set languages = [
        { code: 'en', name: 'English', flag: '🇬🇧' },
        { code: 'fr', name: 'Français', flag: '🇫🇷' }
    ] %}
    
    <details class="dropdown dropdown-end">
        <summary role="button" class="btn btn-ghost m-1">
            {% for lang in languages %}
                <span class="language-flag language-flag-{{ lang.code }} text-lg" style="display: {% if app.request.locale == lang.code %}inline{% else %}none{% endif %}">{{ lang.flag }}</span>
            {% endfor %}
        </summary>
        <ul class="dropdown-content z-[1] menu p-2 shadow bg-base-100 rounded-box w-32">
            {% for lang in languages %}
                <li>
                    <button data-locale="{{ lang.code }}" class="flex items-center gap-2 {% if app.request.locale == lang.code %}active{% endif %}">
                        <span class="text-lg">{{ lang.flag }}</span>
                        <span>{{ lang.name }}</span>
                    </button>
                </li>
            {% endfor %}
        </ul>
    </details>
</div>
